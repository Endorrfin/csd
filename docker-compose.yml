# docker-compose.yml
services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5433:5432'
    environment:
      POSTGRES_DB: csd_db
      POSTGRES_USER: csd_user
      POSTGRES_PASSWORD: csd_password
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
